# ZK Credentials & snarkyjs (mina) fun

### Create and verify ZK Credential with Snarkyjs (mina)

install dependencies:
```shell
npm i
```

compile:
```shell
npx tsc
```

run:
```shell
node .
```